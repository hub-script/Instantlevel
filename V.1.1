-- Fully executable script for Roblox Executors (Delta, KRNL, Fluxus, etc.)

-- Create the ScreenGui
local gui = Instance.new("ScreenGui")
gui.Name = "PetLevelUpUI"
gui.ResetOnSpawn = false
gui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Main frame
local frame = Instance.new("Frame")
frame.Size = UDim2.new(0, 250, 0, 170)
frame.Position = UDim2.new(0.5, -125, 0.5, -85)
frame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
frame.BorderSizePixel = 0
frame.Draggable = true
frame.Active = true
frame.Parent = gui

-- Title
local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, 0, 0, 30)
title.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
title.Text = "🐾 Level Up Your Pets"
title.TextColor3 = Color3.new(1, 1, 1)
title.Font = Enum.Font.SourceSansBold
title.TextSize = 18
title.Parent = frame

-- Username
local user = Instance.new("TextLabel")
user.Size = UDim2.new(1, 0, 0, 25)
user.Position = UDim2.new(0, 0, 0, 30)
user.BackgroundTransparency = 1
user.Text = "User: ScriptByteX"
user.TextColor3 = Color3.fromRGB(255, 100, 100)
user.Font = Enum.Font.SourceSans
user.TextSize = 16
user.Parent = frame

-- Fake Level Up Button
local button = Instance.new("TextButton")
button.Size = UDim2.new(0.8, 0, 0, 40)
button.Position = UDim2.new(0.1, 0, 0, 60)
button.BackgroundColor3 = Color3.fromRGB(255, 70, 70)
button.Text = "🔥 Level Up 50+ Instantly"
button.TextColor3 = Color3.new(1, 1, 1)
button.Font = Enum.Font.SourceSansBold
button.TextSize = 16
button.Parent = frame

-- Pet Info Label
local petInfo = Instance.new("TextLabel")
petInfo.Size = UDim2.new(1, 0, 0, 30)
petInfo.Position = UDim2.new(0, 0, 0, 110)
petInfo.BackgroundTransparency = 1
petInfo.TextColor3 = Color3.fromRGB(180, 255, 180)
petInfo.Font = Enum.Font.SourceSans
petInfo.TextSize = 14
petInfo.Text = ""
petInfo.Parent = frame

-- Created By Label
local createdBy = Instance.new("TextLabel")
createdBy.Size = UDim2.new(1, 0, 0, 20)
createdBy.Position = UDim2.new(0, 0, 1, -20)
createdBy.BackgroundTransparency = 1
createdBy.Text = "Created by Chazai"
createdBy.TextColor3 = Color3.fromRGB(150, 150, 150)
createdBy.Font = Enum.Font.SourceSansItalic
createdBy.TextSize = 14
createdBy.Parent = frame

-- Logic: Check if user is holding a fruit
local function isHoldingFruit()
    local character = game.Players.LocalPlayer.Character
    if not character then return false end
    for _, tool in pairs(character:GetChildren()) do
        if tool:IsA("Tool") and string.lower(tool.Name):find("fruit") then
            return true
        end
    end
    return false
end

-- Fake Pet Generator
local function generateFakePet()
    local names = {"Fluffy", "Shadow", "Whiskers", "Chomp", "Fang"}
    local name = names[math.random(1, #names)]
    local age = math.random(5, 15) .. " days"
    local weight = math.random(10, 80) .. " lbs"
    return "Pet: " .. name .. " | Age: " .. age .. " | Weight: " .. weight
end

-- Button Click
button.MouseButton1Click:Connect(function()
    if isHoldingFruit() then
        petInfo.Text = generateFakePet()
    else
        petInfo.Text = "❌ Hold a fruit to level up!"
    end
end)
